schema-version = "1.12.0"

[install]
# jbayer/sfw is on FloxHub - first activation may need `flox auth login`;
# subsequent activations work offline once the package is in /nix/store.
sfw.pkg-path = "jbayer/sfw"
# nodejs for the `sfw npm install lodahs` demo (see README.md)
nodejs.pkg-path = "nodejs"
# pip for the `sfw pip install fabrice` demo (pulls python3 in as a runtime dep).
# Note: PyPI often takes down flagged malicious packages quickly, so `fabrice`
# may already be gone by the time you try it - seeing sfw wrap pip is the
# demo's main value either way. See README.md for details.
pip.pkg-path = "python313Packages.pip"
# cargo for the `sfw cargo install rustdecimal` demo. No rustc needed:
# the network fetch from crates.io happens before any build step.
# Note: crates.io yanks/removes flagged crates fast (rustdecimal is already
# gone), so the network call may fail with "not found" rather than an sfw
# block - seeing sfw wrap cargo is still the demo's point. See README.md.
cargo.pkg-path = "cargo"

[options]
systems = [
  "aarch64-darwin",
  "aarch64-linux",
]
